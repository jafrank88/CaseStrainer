# Word add-in (legacy copy)

**Deployable source of truth:** `../public/word-addin/` — that folder is what Vite publishes to `/casestrainer/word-addin/` in production.

The files in `src/word-addin/` are an older duplicate; new work should edit `public/word-addin/` so the built site stays in sync.
